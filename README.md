Hello World, I create a docker images and run it on a docker engine using the following command
docker build -t hello-docker .
-t = tag(latest)
hello-docker = docker repository
. = working directory

I ran the images it with:
docker run hello-docker

To check images:
docker images 
OR
docker image ls

THANKS
